/*function() {
    
    var IO = {
        //io stuff
        init: function() {
            IO.socket = io.connect();
            IO.bindEvents();
        };
        
        bindEvents: function() {
            IO.socket.on('giveNumPlayers', IO.updateNumPlayers);

  },

}

IO.init(); */